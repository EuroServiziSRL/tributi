<div>
    <span id="dominio_portale" class="hidden"><%=@dominio%></span>
    <span id="cf_utente_loggato" class="hidden"><%=@cf_utente_loggato%></span>

    <% @assets.each do |asset| %>
        <link rel="stylesheet" href="<%=asset%>" />
    <% end %>

</div>